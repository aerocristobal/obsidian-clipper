{
  "name": "readability-bundle",
  "version": "1.0.0",
  "private": true,
  "description": "Mozilla Readability + linkedom bundled for JavaScriptCore (iOS share extension)",
  "scripts": {
    "build": "esbuild entry.js --bundle --format=iife --global-name=ReadabilityKit --target=es2017 --outfile=../readability-bundle.js"
  },
  "devDependencies": {
    "@mozilla/readability": "^0.5.0",
    "esbuild": "^0.24.0",
    "linkedom": "^0.18.0"
  }
}
